import PhoneInput, {PhoneInputProps} from 'react-native-phone-number-input';

export default function _InternalPhoneInputField({...props}: PhoneInputProps) {
  ///@ts-ignore
  return <PhoneInput {...props} />;
}
